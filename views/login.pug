extends layouts/login-layout.pug

block content
    .loginContainer
        h1 LEXUEOUDE Chat's Login
        form(method="post")
            input(type="text" ,name="logUsername", placeholder="Input your Email or Username", required="")
            input(type="password" ,name="logPassword", placeholder="Input your password", required="")
            input(type="submit", value="Login")
        a(href="/register") Want to join LEXUEOUDE? Click here to Register!!!
